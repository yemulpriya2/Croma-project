# Croma-project
A Django-based web application implementing full CRUD operations with user authentication (Login &amp; Registration). The project uses Django ORM for efficient database management and demonstrates clean backend development practices.
